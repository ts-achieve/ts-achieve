{
  "name": "ts-achieve",
  "repository": {
    "url": "https://github.com/ts-achieve/ts-achieve.git"
  },
  "displayName": "ts-achieve",
  "description": "TypeScript errors as achievements",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.110.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "tsAchieve.show",
        "title": "Show Achievements"
      },
      {
        "command": "tsAchieve.refresh",
        "title": "Refresh Achievements"
      }
    ],
    "configuration": {
      "title": "TS Achieve",
      "type": "object",
      "properties": {
        "tsAchieve.config.notifyRepeatedAchievements": {
          "type": "boolean",
          "default": false,
          "description": "Whether to show information message on achievements that are already earned."
        },
        "tsAchieve.config.revealDescription": {
          "type": "boolean",
          "default": false,
          "description": "Whether a locked achievement should reveal its description."
        },
        "tsAchieve.config.sort": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "kind (errors first)",
              "kind (errors last)",
              "locked first",
              "unlocked first",
              "code (ascending)",
              "code (descending)",
              "alphabetic (forwards)",
              "alphabetic (backwards)"
            ]
          },
          "default": [
            "kind (errors last)",
            "locked first",
            "code (ascending)"
          ],
          "description": "The ordered list of properties by which to sort."
        }
      }
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "achieves",
          "title": "Achievements",
          "icon": "lib/typescript.svg"
        }
      ]
    },
    "views": {
      "achieves": [
        {
          "id": "tsAchieve.view.list",
          "name": "List",
          "icon": "lib/typescript.svg"
        },
        {
          "id": "tsAchieve.view.speedrun",
          "name": "Speedrun",
          "icon": "lib/typescript.svg"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "pnpm run compile",
    "test": "vitest --run && vscode-test --run",
    "p": "pnpm exec vsce-pnpm"
  },
  "devDependencies": {
    "@types/node": "22.x",
    "@types/vscode": "^1.110.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "typescript": "^5.9.3",
    "vsce-pnpm": "^0.1.0"
  },
  "dependencies": {
    "vitest": "^4.1.0"
  }
}
